# Progress

Cập nhật file này ở cuối MỌI task (xem quy trình trong `CLAUDE.md` root).

## Phase hiện tại: P0 (= M0) DONE — Planning xong, chờ duyệt, chuẩn bị P1

### Đã xong (M0)

- Monorepo pnpm + turbo, tsconfig/eslint dùng chung (`packages/config`), husky + lint-staged.
- `packages/shared`: `CardDefinition` (Zod schema) + 5 sample card placeholder.
- `packages/game-engine`: skeleton `GameState`/`PlayerState`/`Zone`/`CardInstance`, seeded RNG
  (mulberry32), `applyAction` với `StartDuel` (shuffle + opening hand) và `Draw`. 6 test xanh.
- `apps/api` (NestJS 11): config Zod validate (fail-fast), Prisma (Postgres, migration `init`
  đã chạy), Pino logger, Swagger (`/docs`), global exception filter, throttler, health check
  thật (`/health` check DB). Module skeleton: auth/users/decks/collections/duels/matchmaking/
  realtime (rỗng, có comment mô tả milestone sẽ implement). `cards` module trả
  `SAMPLE_CARDS` từ shared.
- `apps/web` (Phaser 3 + Vite 7): Boot → Menu (hiển thị trạng thái kết nối API) → Duel scene
  (vẽ board 5+5 zone x2 player bằng placeholder rectangle). Zustand vanilla store cho view state.
- `docker-compose.yml`: Postgres (port **5433**, không phải 5432 — tránh đụng container khác
  trên máy dev) + Redis (port **6380**, chưa wire vào code).
- Verify thật: `pnpm install/lint/typecheck/test/build/dev` xanh toàn workspace;
  `/health` trả `{"status":"ok","database":"ok"}` với DB thật; Vite dev server load được,
  gọi `/health` thành công qua CORS.

### Đang làm / Bị chặn

- **Task 1.1 ✅ xong và đã duyệt** (`RulesetConfig` + `state.ruleset`). Bộ plan `docs/plan/*` đã duyệt.
- **Task 1.1b ✅ xong (2026-09-21, chờ duyệt)** — áp dụng C1/C2/C3: `extraMonsterZones` (0, chỉ lưu), `extraDeckSize` 20, `StartDuel.payload.startingLP` ghi đè LP từng bên. C1–C4, C9, C10, C12 đã đóng (ADR 2026-09-21 trong `DECISIONS.md`). C2/C10 dựa trên giả thuyết.
- **Ingest video #1/#2 xong (2026-09-20)** — mâu thuẫn ở `docs/reference/notes/rules.md` + `rules-observed.md`.

### Bàn giao cho session mới

- **C11 đã chốt (2026-09-20)**: `[DECISION]` Trap phải Set mới kích hoạt, không từ tay; `[RULE]` Trap vừa Set chưa kích hoạt trong lượt đó; `[RULE]` Spell thường kích hoạt từ tay ở Main Phase. Đã thêm `allowTrapActivationFromHand` (false) + `trapSetTurnDelay` (true) vào `RulesetConfig` (shared, có test); hành vi engine ở task 3.4 (test `it.todo` ở `packages/game-engine/src/rules/trap-activation.test.ts`). Quan sát 15:17 → backlog `rules-observed.md`.
- **Task P1 còn lại** (nguồn: `docs/plan/MASTER-PLAN.md`; đọc `RULES-REVIEW-SHEET.md` + `rules-coverage.md` theo nhãn):
  1. **1.2** ✅ xong (2026-09-21, chờ duyệt) — `EndPhase`, `PhaseChanged`/`TurnChanged`; draw khi rời Draw phase (ADR 2026-09-21). `firstTurnAttack` chưa được dùng: `DeclareAttack` (1.6) phải đọc nó.
  2. **1.3** ✅ xong (2026-09-21, chờ duyệt) — `NormalSummon`/`SetMonster` lv 1–4, `resetTurnFlags`, `ActionContext.cardDefinitions` (ADR 2026-09-21).
  3. **1.4** ✅ xong (2026-09-21, chờ duyệt) — Tribute Summon/Set qua `tributeInstanceIds` (`[RULE]` lv5–6:1, lv7+:2), event `MonsterTributed`, ô giải phóng dùng lại được (ADR 2026-09-21). `PendingPrompt SelectTribute` (overlay/Hủy, C9/G2) tách sang task UI, không còn trong 1.4.
  4. **1.5** ✅ xong (2026-09-21, chờ duyệt) — `ChangePosition` (`toPosition` tường minh), event `PositionChanged`, dấu lượt theo quái `summonedTurn`/`positionChangedTurn`/`attackedTurn` trên `CardInstance` (ADR 2026-09-21). `attackedTurn` chỉ được đọc: `DeclareAttack` (1.6) phải ghi. Toàn bộ `[RULE]`, chưa có `[REF]`.
  5. **1.6** ✅ xong (2026-09-22, chờ duyệt) — `DeclareAttack`: direct attack, ATK-vs-ATK, ATK-vs-DEF theo `RULES-REVIEW-SHEET.md` (đã sửa lại brief ban đầu cho khớp dòng 29/33-34 đã duyệt), event `AttackDeclared`/`MonsterDestroyed`/`DamageDealt`, ghi `attackedTurn` (ADR 2026-09-22). Chưa xử lý win condition (LP=0) lúc đó. Lỗ hổng chủ đích: sân đối thủ chỉ có quái úp → không có action tấn công hợp lệ (chờ Flip ở task sau).
  6. **1.7** ✅ xong (2026-09-22, chờ duyệt) — Win condition LP ≤ 0: `checkLifePointsWinCondition` (`state/win-condition.ts`, tái dùng được), gọi từ `declare-attack.ts` sau khi build state cuối; `GameState.winnerIndex` mở rộng `0|1|'draw'|null` để biểu diễn hòa mà không đụng nghĩa "đang đấu" của `null`; event `DuelEnded {winnerIndex, reason: 'LP_ZERO'}` (ADR 2026-09-22). **Đổi số thứ tự task**: brief gọi win condition là "1.7" nên làm trước Flip-on-attack — Flip-on-attack đẩy xuống task kế tiếp (chưa đặt số).
  7. **1.8** ✅ xong (2026-09-23, chờ duyệt) — Flip-on-Attack: target face-down giờ hợp lệ, lật `DefenseDown → DefenseUp` trước damage calc (tái dùng nguyên logic ATK-vs-DEF task 1.6), event `MonsterFlipped` phát trước `MonsterDestroyed`/`DamageDealt`; xoá mã lỗi `TARGET_FACE_DOWN` (ADR 2026-09-23). Direct-attack condition xác nhận đã đúng từ 1.6/1.7, không cần sửa. Đóng lỗ hổng "sân đối thủ chỉ có quái úp → không tấn công được" từ 1.6. Chưa làm Flip Effect thật (chờ effect system P3).
  8. **1.9** ✅ xong (2026-09-23, chờ duyệt) — `Surrender`: bất kỳ bên nào, mọi phase (kể cả khi có prompt treo), chặn `DUEL_ENDED`/`SURRENDER_DISABLED`; đối thủ thắng; event `DuelEnded` thêm `reason: 'SURRENDER'` (không đổi shape). `allowSurrender=false` → `SURRENDER_DISABLED` (ADR 2026-09-23).
     8b. **1.10** ✅ xong (2026-09-23, chờ duyệt) — Deck-out: logic rút đã có, thêm `DuelEnded {reason:'DECK_OUT'}` sau `DeckOut`, guard `DUEL_ENDED` cho `Draw`; 2 assertion cũ đổi (ADR 2026-09-23).
     8c. **1.11** ✅ xong (2026-09-23, chờ duyệt) — Hand limit 6: `EndPhase` từ Main2 với tay > `handLimit` mở `PendingPrompt DiscardToHandLimit` (không tiến phase); action mới `ResolvePendingPrompt` + event `CardDiscarded`; **prompt thật đầu tiên** của engine. `Draw` thêm guard `PENDING_PROMPT` (ADR 2026-09-23).
  9. **1.12** ✅ xong (2026-09-23, chờ duyệt) — Golden replay (5 case, `src/__golden__/*.json`, ghi lại bằng `UPDATE_GOLDEN=1`) + fuzz harness có seed (`src/testing/fuzz`, 10 seed × 300 trong suite; 1000 seed × 1000 chạy tay: 0 vi phạm, không có bug ở 1.1–1.11). Không đổi logic engine (ADR 2026-09-23).

- Tư liệu còn thiếu (Set / đổi thế / Lật, màn thắng-thua, ảnh tab Dung Hợp) **không chặn** 1.4 — xem `parity-board.md`, `human-tasks.md`.
- **Củng cố trước 1.4 ✅ (2026-09-21, chờ duyệt)** — mutation test `summon.ts` (17 đột biến, 0 sống), 4 commit 1.2/1.3 đều xanh độc lập, `EngineError` + `expectEngineError`, `cardDefinitions` bắt buộc (ADR 2026-09-21). Lưu ý: `docs/reference/02-yugi-h5-mechanics.md` chưa có trong repo.
- **Task 2.1 ✅ xong (2026-09-23, chờ duyệt)** — `StateView` filter: type ở `packages/shared/src/duel/state-view.ts`, hàm thuần `toStateView(state, viewerIndex)` ở `apps/api/src/modules/duels/state-view.ts` (12 test, 5/5 mutant bị bắt). Chưa lọc event (để dành). Chặn 2.2/2.3: mọi response chứa state phải qua `toStateView`. Xem ADR 2026-09-23 "StateView".
- **Task 2.2 ✅ xong (2026-09-23, chờ duyệt)** — `DuelManager` thuần (`apps/api/src/modules/duels/duel-manager.ts`) + `DuelStore` (interface async + `InMemoryDuelStore`) + `DuelService` Nest mỏng + `DuelsModule`. `createDuel`/`submitAction`/`getView`/`getDuel`/`closeDuel`; mutex theo duel; lưu seed + action log (replay tái tạo đúng state, có test); lỗi có cấu trúc `DuelServiceError`. 19 test mới (36 api), 9/9 mutant bị bắt. Chưa controller/socket, chưa lọc event. Xem ADR 2026-09-23 "DuelService".
- **Task event-filter ✅ xong (2026-09-23, chờ duyệt)** — `toEventView`/`toEventViews` (`apps/api/src/modules/duels/event-view.ts`, switch vét cạn + `never`, deny by default) + type `EventView` (`packages/shared/src/duel/event-view.ts`); `submitAction` trả `{view, events, eventsByViewer}` (raw không ra ngoài). Kiểm kê 15 event: chỉ `CardDrawn` OWNER_ONLY, còn lại PUBLIC (`docs/design/event-visibility.md`). 26 test mới (62 api), 9/9 mutant bị bắt, probe exhaustiveness xác nhận. Xem ADR 2026-09-23 "Event filter".
- **Task 2.3 ✅ xong (2026-09-24, chờ duyệt)** — HTTP duel solo-debug: `POST /auth/guest` (JWT stateless), `POST /duels/solo`, `GET /duels/:id?viewer`, `POST /duels/:id/actions`; `validateDeck` + `STARTER_DECK` ở `packages/shared` (+15 lá quái placeholder, pool 20 lá); `duel-access.ts` (guest → ghế), `duel-http.ts` (mã lỗi → HTTP), `createDuel` trả view + event mở đầu; session có `mode`/`ownerId`. e2e supertest, 12/12 mutant bị bắt. Xem ADR 2026-09-24 "HTTP duel solo" + `docs/ai/review-packets/task-2.3.md`. Không đụng game-engine.
- **Task 2.4 ⏳ CHỜ NGƯỜI DÙNG TEST (chưa xong)** — smoke test thật đã chạy (Docker + Postgres 5433 + migrate + `pnpm dev`): `tools/smoke-http.ts` 18/18 (`docs/ai/review-packets/task-2.4-smoke.md`), `tools/play-duel.ts` tự chơi tới LP 0 + Surrender 129/129, `HEAVY=1` (nhánh Tribute) 123/123, 0 lộ bài, không lỗi engine. Người dùng làm theo `docs/design/debug-ui.md` (mục A khởi động, C kịch bản, D soi lộ bài, E mẫu báo lỗi). Chưa chụp screenshot (Playwright MCP không kết nối). Hook `.claude/hooks/block-heredoc.js` chặn heredoc có nháy. Phần code: trang debug thô `apps/web/debug.html` (`src/debug/` + `src/api/`, không Phaser) chơi solo-debug qua HTTP; `PlayerActionSchema`/`ActionInputSchema` + `http-types` ở `packages/shared`, API dùng schema → payload méo `400` thay vì `500` (assertion compile-time `PlayerAction` ⊂ engine `Action`); không có `legalActions` nên UI dựng nút từ state, server từ chối = 409 + `engineCode`. 58 test web, 32 schema, 7 ca e2e mới, 11/11 mutant bị bắt. Bước 0 (smoke test boot thật) đã chạy sau khi bật Docker (xem trên). Xem ADR 2026-09-24 "Trang debug solo", `docs/design/debug-ui.md`, `docs/ai/review-packets/task-2.4.md`. **Đổi số task**: AI dummy → 2.5; Duel scene 2.6 … i18n 2.11.
- **Task 2.5 ✅ xong (2026-09-24, chờ duyệt)** — `legalActions`, phục vụ **ba nơi: AI (2.6), trang debug, Phaser (highlight ô hợp lệ)**. Engine: `getLegalActions(state, seat, ctx)` (`packages/game-engine/src/legal-actions.ts`, chỉ THÊM export): sinh ứng viên theo cấu trúc rồi lọc bằng dry-run `applyAction` (không sao chép luật). Shared: `legalActions: PlayerAction[]` trong `ViewResponse`/`GetViewResponse`. API: GET `?viewer=`, POST actions, `POST /duels/solo` trả `legalActions` của đúng ghế; `DuelManager.getLegalActions`. Trang debug: nút ngoài `legalActions` bị mờ/tắt + công tắc "Cho phép thử hành động sai luật" (xem 409). Test: 18 kịch bản engine + property test (8 seed × 200 bước, ~1.4k state/~30k negative, 0 vi phạm), leak test e2e, 17 mutant thủ công (2 tương đương). `tools/smoke-http.ts` 23/23, `tools/play-duel.ts` 109/109 (+`HEAVY=1` 124/124; mọi action gửi ⇔ legalActions). Xem ADR 2026-09-24 "legalActions" + `docs/ai/review-packets/task-2.5.md`. **Đổi số task**: AI dummy + `solo-vs-ai` → 2.6; Duel scene 2.7 … i18n 2.12.
- **Task 2.6 ✅ xong (2026-09-24, chờ duyệt)** — AI rule-based thuần (`apps/api/src/modules/duels/ai/`, chỉ thấy StateView + legalActions) + mode `solo-vs-ai` (người ghế 0, AI ghế 1; ghế AI 403) + driver trong khoá duel qua `applyAndSave` (trần 200 → `AI_LOOP_LIMIT`) + `aiActions` ở response + trang debug "Đấu với AI" + `tools/play-vs-ai.ts` (83/83). 200 ván AI-vs-AI đều kết thúc, AI thắng random 91.5%, 21/21 mutant bị bắt. Không đổi game-engine. Xem ADR 2026-09-24 "AI rule-based" + `docs/ai/review-packets/task-2.6.md`.
- **Task 2.7 ✅ xong (2026-09-24, chờ duyệt; chưa chạy thật với API vì Docker tắt)** — Phaser DuelScene tĩnh + `solo-vs-ai`, chỉ nút bấm ("Phase tiếp theo", "Kết thúc lượt", "Đầu hàng" 2 lần bấm; bỏ bài quá giới hạn bằng cách bấm 1 lá). Lớp thuần `apps/web/src/duel/` (`layout`, `presenter`, `theme`, `asset-manifest`, `fixtures`, `duel-controller`, `detail-text`, `labels`) + scene chỉ vẽ `RenderModel`; placeholder sinh bằng code theo manifest; fixture `?fixture=midgame|handfull|gameover` chỉ ở DEV. 43 test mới (112 web), 12/12 mutant bị bắt; screenshot bằng Edge headless ở `docs/ai/review-packets/task-2.7-screens/`. Không đổi engine/API/shared. **Đổi lộ trình**: 2.8 = kéo thả + highlight, 2.9 = animation theo event + nhịp AI (MASTER-PLAN đã sửa). Xem ADR 2026-09-24 "Duel Scene" + `docs/ai/review-packets/task-2.7.md`.
- **Task 2.8 ✅ xong (2026-09-25, chờ duyệt)** — kéo thả trong DuelScene, chơi trọn ván bằng chuột. Bước 0: 2.7 chạy đúng trên API thật (smoke 23/23, play-vs-ai 142/142, `DuelController` qua fetch thật 3+ lượt + bỏ bài), không có lỗi 2.7. Máy trạng thái tương tác THUẦN (`apps/web/src/duel/interaction.ts`, không Phaser): `idle → dragging-card/dragging-attack → choosing-option → selecting-tribute → pending-server`; mọi action gửi là phần tử của `legalActions` (`legal-index.ts` chỉ lọc/nhóm, không luật), không optimistic, `pending`/`busy` khoá input, bị từ chối → về idle + toast tiếng Việt (`error-messages.ts`). Hit-test thuần ở `layout.ts`; `interaction-driver.ts` nối máy với `DuelController` (`submit` trả `SubmitResult`; fixture chỉ ghi "sẽ gửi: …"). Scene chỉ chuyển pointer events + vẽ overlay (ô hợp lệ, mũi tên, lá ma, menu, tribute, toast). Fixture mới `summon-choice|tribute|attack|attack-direct|drag-illegal`. Test: viết ĐỎ trước (69/147 đỏ, `task-2.8-red.txt`), web 218 test xanh, 19 mutant (18 bắt + 1 tương đương), e2e thật `tools/play-duel-ui.ts` (aggressive thắng, có 3 tribute qua UI; hoarder bỏ bài; turtle đổi thế) và `tools/ui-drag-shots.ts` (chuột thật trong Edge qua CDP). Không đổi engine/API/shared. Xem ADR 2026-09-25 "Máy trạng thái tương tác" + `docs/ai/review-packets/task-2.8.md`.
- **Task 2.9 ✅ xong (2026-09-25, chờ duyệt)** — animation theo `EventView[]` + phát lại `aiActions` từng bước, chỉ `apps/web` (engine/API/shared 0 dòng đổi). Board giữ view CŨ, hiệu ứng tạm vẽ chồng, xong mới snap `response.view`. Lớp thuần `animation-queue.ts` (`stepsFor`/`segmentsFor`, vét cạn `never`) + `animation-player.ts` (scheduler tiêm được, `skip`/`speed`, `AnimatorHost`); `duel-controller.ts` thêm `animator?`/`animating`/`skipAnimation()`; scene vẽ hiệu ứng, phím Space/Enter = skip, `?fast=1`/`?anim=off`. Khoá input tái dùng `busy` (không thêm kind `interaction.ts`). Test: đỏ trước (10 fail + 3 file không chạy, `task-2.9-red.txt`), web 269 test xanh (218 → +51), 18/18 mutant bị bắt (`task-2.9-mutants.txt`), e2e thật `play-duel-ui` 4/4 + `play-vs-ai` 60/60 (baseline 45/45), smoke 23/23. **Chưa** chụp screenshot hiệu ứng (chỉ kiểm bằng test + build). Thời lượng đã hiệu chỉnh gần bản gốc (chủ dự án chốt, xem ADR "Hiệu chỉnh `DURATION_MS`"). Xem ADR 2026-09-25 "Animation theo event".
- **Task 2.10 ✅ xong (2026-09-25, chờ duyệt)** — log panel trong DuelScene: bật/tắt (nút "Ẩn" ↔ tab "Log", phím L) + lọc 4 nhóm (Sân/Đánh/Lượt/Lỗi + chip "Tất cả"), trạng thái nhớ ở localStorage. Chỉ `apps/web` (engine/API/shared, `debug-state.ts`, `debug-page.ts`, animation: 0 dòng đổi). Hướng (b): `log: string[]` giữ nguyên, thêm `entries: LogEntry[]` song song (`log-entries.ts`, `log-panel.ts` thuần; category từ `EventView['type']`/`PlayerAction['type']`, vét cạn `never`). Test: đỏ trước (`task-2.10-red.txt`), web 330 test xanh (269 → +61, gồm toàn bộ test debug), 13/13 mutant bị bắt (1 sống lần đầu → thêm test slice chồng lấn), build xanh, screenshot thật ở `docs/ai/review-packets/task-2.10-screens/` (`tools/ui-log-shots.ts`, API thật). Xem ADR 2026-09-25 "Log panel" + `docs/ai/review-packets/task-2.10.md`.
- **Task 2.11 ✅ xong (2026-09-25, chờ duyệt)** — Duel Sandbox (dev-only). Shared: `ScenarioSchema` + 3 scenario mẫu `packages/shared/scenarios/{tribute-summon,attack-defense,chain-basic}.json`. API: `POST /dev/sandbox/duels` (guest token; `?mode=`), `scenarioToState` thuần dựng `GameState` ở api (**engine 0 dòng đổi**), `DuelManager.createDuelFromState` (script qua `applyAndSave`, session có `initialState` → replay được), module chỉ mount khi NODE_ENV != production (production → 404, có e2e). Web: `/dev/sandbox.html` (textarea + chọn mẫu + Nạp, tái dùng nguyên `DuelScene`; không nằm trong build production), `DuelController.startScenario`. **chain-basic chỉ dựng state đầu vì P3 chưa có Spell/Trap/Chain.** Test đỏ trước (`task-2.11-red.txt`); mutant thủ công (`task-2.11-mutants.txt`); smoke HTTP thật (`task-2.11-smoke.md`); screenshot thật `task-2.11-screens/`. Xem ADR 2026-09-25 "Duel Sandbox" + `docs/ai/review-packets/task-2.11.md`.
- **Planning: mở rộng scope P10–P15 (2026-09-25, chỉ tài liệu, chờ chủ dự án duyệt hướng)** — Economy, Gacha, Shop, Adventure, Arena, Live-ops đã vào `MASTER-PLAN.md` (phase-level, **chưa** breakdown task con), `economy-plan.md`, `modes-and-liveops-plan.md`, `project-vision-patch.md` (bản vá cho `01-project-vision.md` ngoài repo), ADR 2026-09-25 "Mở rộng scope". Cần chủ dự án chốt: các `[DECISION]`/`[CẦN HỎI]` (E1–E9 + mục P13–P15) và duyệt **đề xuất sửa CLAUDE.md #5**. Không đổi code/thứ tự task hiện tại.
- **Task tiếp theo: 2.12** (i18n, theo MASTER-PLAN). **Cổng bắt buộc trước P3**: fuzz/golden kiểm bất biến "không lộ thông tin" qua HTTP khi có Spell/Trap. Review P1 vẫn cần người duyệt.
- **Việc để dành (backlog P2+):** (1) `validateDeck` trong `packages/shared` (40–60 lá, ≤3 bản; dùng ở 2.3/P7); (2) TTL/dọn duel bỏ dở + store bền (DB/Redis; mutex hiện chỉ đúng 1 process); (3) Extra Deck của chủ sở hữu cho task Fusion (hiện `StateView` chỉ có `extraDeckCount`); (4) lịch sử event/resync (event chưa lưu vào session; client mất event = phải GET view); (5) ~~event mở đầu chưa trả từ `createDuel`~~ **xong ở 2.3**; (6) **cổng bắt buộc trước P3**: fuzz/golden kiểm bất biến "event/view không lộ definitionId ẩn" khi P3 có Spell/Trap (e2e 2.3 mới có 1 ván kịch bản, chưa fuzz); (7) TTL/dọn duel bỏ dở (`InMemoryDuelStore` mất phiên khi restart; guest token 12h không refresh); (8) Extra Deck của chủ cho Fusion; (9) schema Action đầy đủ ở `packages/shared` (HTTP mới kiểm phần vỏ; payload méo có thể ra `500` thay vì `400`); (10) guest chưa lưu DB (upgrade lên account = P7).
- **Ánh xạ số task P1** (MASTER-PLAN gốc → thực tế): 1.7 gốc (Flip) → 1.8; 1.8 gốc (Win/Surrender/hand-limit) → tách 1.7 win + 1.9 Surrender + 1.10 deck-out + 1.11 hand-limit; 1.9 gốc (golden/fuzz) → 1.12.
- Đã chốt sau duyệt 1.1: `openingHandSize = 5` (**[REF]**, video #2 4/4 ván), `afkLossThreshold = 3`, `extraDeckSize = 20` (**[REF thấp, 1 nguồn]**, C3), `startingLP = 8000` (**[DECISION]**, C2/C10), `extraMonsterZones = 0` (C1).
- G1–G8/G11/G12 đã chốt (xem `docs/reference/notes/rules.md`); G9/G10 vẫn `[GUESS]` chờ tư liệu.
- Port Postgres/Redis đã đổi (5433/6380) — dùng `apps/api/.env.example` làm chuẩn.
- `apps/api` build bằng `tsc` trực tiếp (xem `docs/ai/DECISIONS.md`).
- Mỗi task kết thúc bằng Review Packet (`/review-packet`) và cập nhật `docs/plan/parity-board.md`.

## Checklist phase (P0–P9) — chi tiết task: `docs/plan/MASTER-PLAN.md`

- [x] P0 — Setup monorepo + tooling (= M0)
- [x] P1 — Engine core vanilla + RulesetConfig + golden replay/fuzz (bản nháp, chờ duyệt; task 1.1–1.12)
- [ ] P2 — Vertical slice: solo vs AI dummy (API + FE Duel + Sandbox) — 2.1, 2.2, event-filter, 2.3 xong (nháp), 2.4 (trang debug) chờ người dùng test, 2.5 (legalActions) + 2.6 (AI, solo-vs-ai) xong (nháp); 2.7 (Phaser Duel Scene tĩnh) + 2.8 (kéo thả) xong (nháp); còn 2.12; 2.11 (Sandbox) xong nháp (2.8 = kéo thả, 2.9 = animation; 2.4 = trang debug, 2.5 = legalActions, AI dummy → 2.6, i18n → 2.12)
- [ ] P3 — Effect system + Chain + 10 card mẫu
- [ ] P4 — Card batches + Special/Equip/Field/Counter/Fusion
- [ ] P5 — Asset pipeline + Card Gallery
- [ ] P6 — Animation + Audio tier 1 + Animation Preview + Replay Viewer
- [ ] P7 — Auth + Deck Builder + Collection
- [ ] P8 — AI rule-based
- [ ] P9 — PvP private + PvE nhẹ + Polish
- [ ] P10 — Economy & Inventory _(scope mở rộng 2026-09-25, chưa breakdown; sau P7)_
- [ ] P11 — Gacha / Pack opening _(sau P10; cần P4/P5)_
- [ ] P12 — Shop _(sau P11)_
- [ ] P13 — Adventure / Campaign _(mở rộng 9.5; sau P8 + P10)_
- [ ] P14 — Arena (PvP ladder) _(sau P9 + P10; khác Room private 9.1)_
- [ ] P15 — Live-ops _(cuối; đua top dùng Leaderboard P14)_

> Ánh xạ số task P1 (MASTER-PLAN gốc → thực tế): 1.7 gốc (Flip) → 1.8; 1.8 gốc (Win/Surrender/hand-limit) → tách 1.7 win + 1.9 Surrender + 1.10 deck-out + 1.11 hand-limit; 1.9 gốc (golden/fuzz) → 1.12.

Tiêu chí done từng phase: `docs/ai/ROADMAP.md`.
